export const metadata = {
  title: "Job Form Submission",
  robots: {
    index: false,
    follow: false,
  },
};

export default function JobFormSubmissionLayout({ children }) {
  return <>{children}</>;
}
